// tasks.json
{
  "version": "2.0.0",
  "tasks": [
    // run make
    {
      "label": "arara build",
      "type": "shell",
      "command": "arara report",
      "group": {
        "kind": "build",
        "isDefault": true
      }
    },
  ]
}
